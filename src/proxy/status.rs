//! `lcc proxy status / logs / doctor` — V1 basique. (Task 5.2)

use std::io;

pub fn run_status() -> io::Result<()> {
    eprintln!("⚠ status pas encore implémenté (Task 5.2)");
    Ok(())
}

pub fn run_logs(_follow: bool) -> io::Result<()> {
    eprintln!("⚠ logs pas encore implémenté (Task 5.2)");
    Ok(())
}

pub fn run_doctor() -> io::Result<()> {
    eprintln!("⚠ doctor pas encore implémenté (Task 5.2)");
    Ok(())
}
